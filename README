A React CRUD applications.

this.setState({
   foodItems: [...this.state.foodItems, foodItem]
})




render() {
  return (
    this.props.showMe ? <button type="submit" className="btn nav-btn-red">SIGN UP</button> : null
  );
}
